[project]
name = "image-processing"
version = "0.1.0"
description = "A modular pipeline for image processing to be used in ComfyUI"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "black>=26.1.0",
    "pyrefly>=0.52.0",
]

[tool.pyrefly]
# files or directories to exclude from type checking
project-excludes = [
    "external/*",
]
